fn printi32(n: i32): void {
    (printf "%d\n", n)
}

struct Vec<T> {
  size: usize,
  capacity: usize,
  memory: *T
}

fn main(): i32 {
  let dummy:i32 = 0
  let dummy:i32 = 0
  let size: usize = 4
  let buf: *i32 = (malloc size)
  buf[0] = 10
  buf[1] = 20
  (printi32 *buf)
  (printi32 buf[1])
  return 0
}
