// Test generic type inference step by step

fn test_inference(): void {
  (:= v : Vec<i32> [1, 2, 3])

  // Try each call one at a time
  (printf "len=%d\n" (Vec::len v))
  (printf "v[0]=%d\n" (Vec::get v 0))
  //(Vec::set v 0 100)
  //(printf "first=%d\n" (Vec::first v))
}

alloc fn main(): void {
  (test_inference)
}
